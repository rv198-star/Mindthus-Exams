不建议再加第四个分支，尤其是要上线时。

更小、更稳的上线改法是：不新增分支，只把现有邮箱 local-part 的字符集补上 `+`。这属于等量替换，风险比“再挂一个 case 分支”低，也不会继续扩大分支树。

例如把类似：

```regex
[A-Za-z0-9._%-]+
```

改成：

```regex
[A-Za-z0-9._%+-]+
```

然后只加一个回归用例：

```text
user+alias@example.com
```

这样满足 plus 别名上线需求，同时避免把邮箱校验继续补成一串特例。